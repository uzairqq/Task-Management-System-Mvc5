@using System.Web.Optimization
@model TodoApp.Models.Tasks

@{
    ViewBag.Title = "title";
    //Layout = "Index.cshtml";
}

<h2>New</h2>
@using (Html.BeginForm("Save", "Task"))
{
    @Html.LabelFor(i => i.TaskName)
    @Html.TextBoxFor(i => i.TaskName)

    @Html.LabelFor(i => i.Description)
    @Html.TextBoxFor(i => i.Description)


    <button type="submit" class="btn btn-primary">Save</button>
}

